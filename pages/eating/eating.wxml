     <import src="template/template.wxml" />
      <view class="star" wx:for="{{eating}}" >
          <image src="{{item.image}}"></image>
          <view class="star-introduce">
            {{item.text}}
          </view>

        </view>
        
          <template is="msgItem" data="{{...item}}" />